# ============================================
# DORA Docker Environment Variables
# ============================================
# This file is used by docker-compose.yml
# Copy this file to .env and fill in your actual values

# ============================================
# Google OAuth Configuration (REQUIRED)
# ============================================
# Get these from: https://console.cloud.google.com/
# 1. Create a project
# 2. Enable Google Drive API and Google Docs API
# 3. Create OAuth 2.0 credentials
# 4. Add authorized redirect URIs:
#    - http://localhost:3000/auth/callback (development)
#    - https://yourdomain.com/auth/callback (production)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# ============================================
# AI/LLM Configuration (REQUIRED)
# ============================================
# Primary LLM Provider: groq (recommended) or gemini
LLM_PROVIDER=groq

# Groq API Configuration (RECOMMENDED - Free & Fast)
# Get API key from: https://console.groq.com/
# 1. Sign up for free account
# 2. Create API key
# 3. Free tier: 30 requests/minute
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# Gemini API Configuration (OPTIONAL - Alternative)
# Get API key from: https://aistudio.google.com/
# Only needed if you want to use Gemini instead of Groq
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash

# ============================================
# CORS Configuration (REQUIRED)
# ============================================
# Allowed origins for CORS (comma-separated)
# Development:
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Production (uncomment and modify):
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================
# Performance Configuration (OPTIONAL)
# ============================================
# Bulk upload batch size - adjust based on your system RAM
# For 16GB+ RAM: 10
# For 8GB RAM: 5
# For 4GB RAM: 3
BULK_UPLOAD_BATCH_SIZE=10

# ============================================
# Logging Configuration (OPTIONAL)
# ============================================
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=WARNING

# ============================================
# Environment (OPTIONAL)
# ============================================
# Options: development, production
ENVIRONMENT=development

# ============================================
# Supabase Configuration (OPTIONAL - Not currently used)
# ============================================
# Uncomment these if you want to use Supabase in the future
# Get these from: https://supabase.com/dashboard
# SUPABASE_URL=your_supabase_url_here
# SUPABASE_ANON_KEY=your_supabase_anon_key_here
# SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ============================================
# Notes
# ============================================
# - GROQ_API_KEY is the primary AI provider (recommended)
# - GEMINI_API_KEY is optional (alternative to Groq)
# - Supabase is not currently used but available for future features
# - For Docker deployment, all values will be passed to containers
# - Never commit .env file to git (it's in .gitignore)
